# ================================================================
# Bank Sultra - Absensi MSDM
# Environment Configuration Template
# ================================================================
# Copy file ini ke .env dan sesuaikan values

# ----------------------------------------------------------------
# Application
# ----------------------------------------------------------------
NODE_ENV=development
PORT=5000

# ----------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,https://app.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# ----------------------------------------------------------------
# Timezone
# ----------------------------------------------------------------
TZ=Asia/Makassar

# ----------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------
DATABASE=development
CONFIG_ENC_KEY=your-secret-key-minimum-32-characters-change-this
DB_TIMEZONE=+08:00

# ----------------------------------------------------------------
# AES Encryption Configuration (CRITICAL - CHANGE IN PRODUCTION!)
# ----------------------------------------------------------------
# AES_SECRET_KEY must be exactly 32 characters for AES-256
# AES_SECRET_IV must be exactly 16 characters
# Generate secure random keys for production!
AES_SECRET_KEY=rahasiaabsen12345678901234567890
AES_SECRET_IV=iv_rahasiaabsen
DB_POOL_MAX=20
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000
DB_POOL_EVICT=1000

# ----------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------
LOG_ROOT=logs
LOG_LEVEL=info
LOG_FILE_MAXSIZE=10m

# ----------------------------------------------------------------
# Konfigurasi Cron Jobs
# ----------------------------------------------------------------
# Kontrol global untuk mengaktifkan/menonaktifkan semua cron jobs
# Set false untuk menonaktifkan SEMUA cron jobs (berguna untuk development/testing)
CRON_ENABLED=true

# Kontrol Individual Cron Job
# Format: CRON_<JOB_ID>_ENABLED=true/false
# Jika tidak diset, akan menggunakan nilai default dari cronJobs.config.js

# Rekonsiliasi Absensi Harian (berjalan setiap hari jam 02:00)
CRON_REKONSILIASI_HARIAN_ENABLED=true

# Generate Laporan Lembur Bulanan (berjalan setiap tanggal 1 jam 01:00)
CRON_GENERATE_LEMBUR_BULANAN_ENABLED=true

# Generate Shift Harian Bulanan (berjalan setiap tanggal 1 jam 00:00)
CRON_GENERATE_SHIFT_BULANAN_ENABLED=true

# Test Cron Job (berjalan setiap 10 detik - hanya untuk testing)
# PENTING: Harus disabled di production!
CRON_TEST_CRON_ENABLED=false 